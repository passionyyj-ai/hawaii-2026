# TravelMate AI v22.1

- 영어 녹음 결과는 `English → Korean`으로 고정했습니다.
- 한국어 녹음 결과는 `Korean → English`로 고정했습니다.
- 번역 서버 요청에 `mode: translate`를 추가했습니다.
- `sourceLanguage`와 `targetLanguage`를 명시해 번역 방향이 뒤집히는 문제를 방지했습니다.
- 서비스워커 캐시를 `travelmate-v22-1-translation-direction`으로 변경했습니다.
